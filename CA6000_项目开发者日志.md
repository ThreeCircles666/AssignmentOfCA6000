# CA6000 项目开发者日志（中文详细版）

本文是 `CA6000_Project_Work_Log.md` 的中文详细版，按当前代码逐项记录选题、数据检查、清洗和已完成的探索性分析。图和参数以 `notebooks/` 中实际实现以及 `outputs/figures/` 中已保存文件为准，不以某一段 Markdown 计划文字代替最终代码。

状态日期：2026-09-16。当时阶段：数据检查、清洗、EDA 已完成；特征工程和建模尚未开始。

2026-09-17 补记：特征工程、建模评估和文件整理已经完成。第 1–11 节保留当时的开发过程，不删除。最终阶段记录见第 12 节。

------------------------------------------------------------------------

## 1. 选题与任务定义

题目是 Rossmann 门店日销售额预测。数据来自 Kaggle Rossmann Store Sales。

项目要做的不是单纯描述销量，而是沿一条完整分析链走到神经网络：

1. 理解原始数据。
2. 检查数据质量。
3. 只修正已被证据支持的问题。
4. 用 EDA 判断哪些变量值得进入模型。
5. 构造预测特征。
6. 做基线模型和神经网络，并评价结果。

对应文件：

| 阶段 | 文件 | 状态 |
|---|---|---|
| 项目初始化 | `setup_project.py` | 已完成，只补缺失文件，不覆盖已有内容 |
| 数据检查 | `notebooks/01_check.ipynb` | 已完成 |
| 清洗 | `notebooks/02_clean.ipynb` | 已完成 |
| EDA | `notebooks/03_eda.ipynb` | 已完成 |
| 特征工程 | `notebooks/04_features.ipynb` | 仅有计划注释 |
| 建模 | `notebooks/05_model.ipynb` | 仅有计划注释 |

`04_features.ipynb` 的占位注释写的是“构建门店月度建模数据”。官方测试集却是日粒度。这个冲突必须在写特征代码前定下来，见第 10 节。

------------------------------------------------------------------------

## 2. 数据、目录和运行约定

### 2.1 原始表

`data/raw/train.csv`

- 1,017,209 行，9 列。
- 字段：`Store`, `DayOfWeek`, `Date`, `Sales`, `Customers`, `Open`, `Promo`, `StateHoliday`, `SchoolHoliday`。
- 时间范围：2013-01-01 至 2015-07-31，942 个连续日历日，没有缺日。

`data/raw/store.csv`

- 1,115 行，10 列。
- 字段：`StoreType`, `Assortment`, `CompetitionDistance`, `CompetitionOpenSinceMonth`, `CompetitionOpenSinceYear`, `Promo2`, `Promo2SinceWeek`, `Promo2SinceYear`, `PromoInterval`。

`data/raw/test.csv`

- 41,088 行，8 列。
- 字段：`Id`, `Store`, `DayOfWeek`, `Date`, `Open`, `Promo`, `StateHoliday`, `SchoolHoliday`。
- 时间范围：2015-08-01 至 2015-09-17。
- 没有 `Sales`，也没有 `Customers`。
- `Open` 有 11 个缺失；其余字段无缺失。`Open` 取值：营业 35,093，关闭 5,984，缺失 11。

`data/raw/sample_submission.csv` 只有 `Id` 和 `Sales`。提交目标是门店日销售额。

清洗产物：

- `data/processed/train_clean.csv`
- `data/processed/store_clean.csv`

图保存在 `outputs/figures/`。原始文件不覆盖。Notebook 从 `notebooks/` 运行，项目根目录用 `Path.cwd().parent`。

### 2.2 依赖

`requirements.txt` 目前只有：

- pandas 3.0.3
- numpy 2.5.1
- matplotlib 3.11.1
- scikit-learn 1.9.0

神经网络库尚未加入。Matplotlib 3.11 已把 `boxplot(vert=...)` 标为弃用，箱线图代码里仍在使用，见第 8 节注意事项。

### 2.3 Notebook 写法

每个分析步骤按这个顺序写：

1. Markdown：分析什么、为什么要做、用什么方法、为什么合适、和上一结论如何衔接、变量和范围、必要时写公式。
2. Code：只实现上一段写过的方法，并保存需要的图。
3. 每个 Notebook 结尾应有独立总结。检查和清洗笔记本有总结。EDA 笔记本还没有结尾总结，结论记在本文和英文工作日志里。

图的优先级是：先正确表达数据，再保持干净的 SCI 风格。低饱和粉彩色、少装饰、标签可读、图在笔记本中显示，同时另存 PNG。

------------------------------------------------------------------------

## 3. 数据检查：方法和结论

`01_check.ipynb` 只检查，不改数据。共 14 个分析块。

### 3.1 加载与结构

方法：`pathlib.Path` 拼相对路径，`pandas.read_csv()` 读取。不用低层 `csv` 模块，因为后续需要按列统计、缺失值和类型检查。加载时不手工指定类型，让 Pandas 推断，以便发现类型问题。

成功标准：两个文件都存在、可解析、非空、结果是 DataFrame。

结果：train 为 1,017,209 × 9，store 为 1,115 × 10。读取 train 时出现 `DtypeWarning`，随后单独检查，不在加载时用 `dtype` 把警告藏起来。

### 3.2 `StateHoliday` 表示不一致

方法：看列的 dtype、`drop_duplicates()` 后的原始值、`repr()`、`type()` 和精确计数。不用普通 `print`，因为整数 `0` 和字符串 `"0"` 打印出来会看起来一样。检查阶段不转换成 `str` 或 `category`。

结果：

- 字符串 `"0"`：855,087 行。
- 整数 `0`：131,072 行。
- `a`、`b`、`c` 是字符串。

这是同一类别的表示不一致，不是非法类别。留到清洗阶段统一。

### 3.3 缺失值

train 没有缺失值。store 的缺失集中在 Promo2 和竞争相关字段。

方法不是直接 `fillna`，而是按另一个变量做交叉检查：

- 按 `Promo2` 分组，检查 `Promo2SinceWeek`、`Promo2SinceYear`、`PromoInterval` 是否同时缺失、部分缺失或同时存在。
- 检查竞争距离、开业月份、开业年份是否成对缺失。

结果：

- `Promo2 = 0` 的 544 家门店，三个细节字段全部缺失。
- `Promo2 = 1` 的 571 家门店，三个细节字段全部存在。
- 这是结构性缺失：不参加 Promo2，就没有开始周、开始年和间隔，不能用均值填。
- 竞争开业月和年同时缺失 354 家，没有“只缺月”或“只缺年”。
- 其中 351 家仍有有效 `CompetitionDistance`，说明知道有竞争对手，但不知道开业时间。
- `CompetitionDistance` 本身只缺 3 家。

### 3.4 重复与键唯一性

方法：

- `duplicated()` 查整行重复。
- `duplicated(subset=..., keep=False)` 查业务键。train 的键是 `Store + Date`，store 的键是 `Store`。
- `keep=False` 把同一键的所有行都标出来，而不是只标后出现的一行。
- 不在检查阶段 `drop_duplicates()`，因为删除和检测不是同一件事。

结果：整行重复为 0，业务键重复为 0。不需要去重。

### 3.5 类别、数值范围和日期

类别域检查覆盖 `DayOfWeek`、`Open`、`Promo`、`StateHoliday`、`SchoolHoliday`、`StoreType`、`Assortment`、`Promo2`。全部落在合法取值内。

数值范围用业务规则，不用 z-score 或 IQR。这一步要找的是物理上不可能的值，不是统计上少见的值。`Sales`、`Customers`、`CompetitionDistance` 不允许为负。月份上界 12，周数上界 53。月、周、年在有值时是整数。

日期方法：按 `YYYY-MM-DD` 解析，检查不可解析值、最早最晚日期、区间内是否缺日历日，再用日期反推星期，和 `DayOfWeek` 对比。

结果：1,017,209 个日期全部可解析，没有缺日，没有星期不匹配。`Date` 在原始文件里是字符串，清洗时再转 datetime。

### 3.6 业务逻辑和零销售复核

检查关闭门店是否出现正销售或正客流。没有发现系统性逻辑错误。

营业但销售为 0 的记录有 54 条：

- 52 条客流也是 0。
- 2 条客流大于 0。

再按门店和日期看是否集中出现。没有强的系统性错误证据，标为复核案例并保留。

### 3.7 门店编号一致性

提取两边 `Store` 集合，做双向差集。两边都是 1,115 个门店，集合相同。`Store` 可以安全作为合并键。

### 3.8 IQR 离群点筛查

方法：只对营业日的 `Sales`、`Customers`，以及对非缺失的 `CompetitionDistance`，使用 1.5 倍 IQR 规则。

$$
IQR = Q_3 - Q_1
$$

$$
Lower = Q_1 - 1.5 \times IQR
$$

$$
Upper = Q_3 + 1.5 \times IQR
$$

另外查看最大的若干条销售、客流和竞争距离，确认极端值是否仍可能是真实经营结果。

结果：

- 营业日 Sales 离群比例 3.64%。
- 营业日 Customers 离群比例 4.84%。
- `CompetitionDistance` 离群比例 9.71%。
- 高销售常常伴随高客流，数值仍在可能范围内。

结论：这是统计极端，不是确认错误。不自动删除。

### 3.9 检查阶段的行动分类

| 发现 | 分类 | 下一步 |
|---|---|---|
| `StateHoliday` 的 0 有两种类型 | 确认问题 | 统一成一种类别表示 |
| Promo2 细节缺失 | 结构性缺失 | 保留“不适用”，不做统计填补 |
| 竞争开业日期缺失 | 信息缺失 | 显式处理，不编造日期 |
| 竞争距离缺失 3 家 | 信息缺失 | 清洗或预处理时显式处理 |
| 重复记录 | 无问题 | 不删除 |
| 类别域和数值范围 | 无问题 | 不修正 |
| 日期一致性 | 无问题 | 清洗时转 datetime |
| 门店编号 | 无问题 | 可作为合并键 |
| 营业零销售 54 条 | 复核案例 | 保留 |
| IQR 离群点 | 统计离群 | 保留，留给 EDA 和建模 |

------------------------------------------------------------------------

## 4. 清洗：方法和参数

`02_clean.ipynb` 独立于检查笔记本运行，不复用内存中的 DataFrame。原则是：原始文件不变，工作副本上清洗，验证通过后再写出 processed 文件。

流程是：Clean → Validate → Save → Reload → Validate again。

### 4.1 加载和副本

- 再次从 `data/raw/` 读取。
- `train_clean = train.copy()`，`store_clean = store.copy()`。
- 用 `is` 确认副本不是同一对象。

### 4.2 统一 `StateHoliday`

方法：整列转为 Pandas `string`，不填值、不删行。

清洗后类别只有 `0`、`a`、`b`、`c`。两类 0 合并后频数不变，没有新的缺失或非法值。

### 4.3 转换 `Date`

方法：`pd.to_datetime`，格式 `YYYY-MM-DD`，`errors="raise"`。因为检查阶段已确认全部日期合法，这里若解析失败应直接中断，而不是静默变成 `NaT`。

保留：全部行、2013-01-01 至 2015-07-31、与 `DayOfWeek` 的一致性。

### 4.4 Promo2 结构性缺失

不填补。只改类型：

- `Promo2SinceWeek` → 可空 `Int64`
- `Promo2SinceYear` → 可空 `Int64`
- `PromoInterval` → `string`

可空整数是必要的。普通 `int` 不能容纳缺失值，转类型时会被迫填充或失败。

再检查一次：544 家 `Promo2=0` 仍全部缺失细节，571 家 `Promo2=1` 仍完整。

### 4.5 竞争信息

不填距离，不编造开业日期。类型：

- `CompetitionDistance` → 可空 `Float64`
- `CompetitionOpenSinceMonth` → 可空 `Int64`
- `CompetitionOpenSinceYear` → 可空 `Int64`

保留缺失：距离 3，开业月 354，开业年 354。月和年仍然成对缺失。

### 4.6 明确不做的事

- 不删零销售复核记录。
- 不按 IQR 删极端值。
- 不做统计插补。
- 不改未列入清洗清单的字段。

### 4.7 写出与回读

写出参数：

- `to_csv(index=False)`，避免把行号写成新列。
- train 的日期格式 `date_format="%Y-%m-%d"`。

回读时显式恢复类型，因为 CSV 不保存 Pandas 元数据：

- train：`StateHoliday="string"`，日期再按 `%Y-%m-%d` 转回 datetime。
- store：距离 `Float64`，竞争和 Promo2 的周、月、年 `Int64`，`PromoInterval="string"`。

验证通过的内容：行列数、门店集合、业务键唯一、缺失计数、`StateHoliday` 类别、日期边界、可空数值类型。

------------------------------------------------------------------------

## 5. EDA 总原则和分析范围

EDA 回答建模前必须知道的事：分布形状、时间规律、促销和节假日是否真有差异、门店属性是否有稳定差异、竞争距离是否值得做成线性特征。

除特别说明外，分布和组间比较都限制在 `Open = 1`。关闭日的 0 是结构零，不是普通低销售。若把关闭日放进直方图或箱线图，零会形成一个不代表经营表现的峰，并拉低均值。

营业日共 844,392 条，占 83.01%。关闭日 172,817 条，占 16.99%。

后续建模仍应保留关闭日，并在已知 `Open = 0` 时直接把销售额设为 0。EDA 把它们拿开，只是为了看经营日的需求形态。

------------------------------------------------------------------------

## 6. EDA 全局可视化配置

这些参数在 `03_eda.ipynb` 开头设定，作为默认风格。后面若干图会覆盖 figsize 和 dpi。

### 6.1 颜色

| 名称 | 色值 | 用途 |
|---|---|---|
| PASTEL_BLUE | `#AFCBFF` | 销售直方图 |
| PASTEL_GREEN | `#B7E4C7` | 调色板备用 |
| PASTEL_PURPLE | `#D9C2F0` | 调色板备用 |
| PASTEL_YELLOW | `#F6E7A1` | 调色板备用 |
| PASTEL_PINK | `#F7C6D9` | 调色板备用 |
| PASTEL_ORANGE | `#F7D6A3` | 直方图最大值标记 |
| DARK_GREY | `#4A4A4A` | 文字、均值参考线 |
| MID_GREY | `#7A7A7A` | 坐标轴边框 |
| LIGHT_GREY | `#EAEAEA` | 网格 |

后面的分组图没有全部复用这六个变量，而是按图手写了相近的浅色。同一类对比里，不同组用不同浅色，避免只用一种颜色把组别混在一起。

### 6.2 Matplotlib 默认参数

```text
figure.figsize = (8, 5)
figure.dpi = 120
figure.facecolor = white
axes.facecolor = white
axes.edgecolor = #7A7A7A
axes.labelcolor = #4A4A4A
axes.grid = True
grid.color = #EAEAEA
grid.linewidth = 0.7
grid.alpha = 0.7
axes.spines.top = False
axes.spines.right = False
font.size = 10
axes.titlesize = 12
axes.labelsize = 10
legend.frameon = False
```

去掉上、右边框，是为了减少装饰、把视线留在数据上。网格用很浅的灰，避免盖住柱和线。

多数结果图在 `savefig` 时用 `dpi=300`、`bbox_inches="tight"`。`tight` 防止标题、旋转刻度和注释被裁掉。显示用的默认 dpi 是 120，存档用 300，是为了笔记本里不要过大，报告里又足够清晰。

注意：有几张图在 `plt.figure` 或 `plt.subplots` 里又写了 `dpi=300`，再在 `savefig` 里写一次 300。图窗 dpi 已经是 300 时，屏幕尺寸会变得很大。这不影响分析结论，但会让 PNG 像素偏大。

### 6.3 读取参数

train：

- `StateHoliday` 指定 `string`。
- `Date` 用 `format="%Y-%m-%d"`、`errors="raise"`。

store：

- `CompetitionDistance`: `Float64`
- `CompetitionOpenSinceMonth` / `Year`: `Int64`
- `Promo2SinceWeek` / `Year`: `Int64`
- `PromoInterval`: `string`

后面若干 EDA 单元为了能单独重跑，又从 `../data/processed/train_clean.csv` 读了一次。其中没有指定 `dtype` 的读取会触发 `StateHoliday` 混合类型警告。分析前这些单元大多又做了 `astype(str)` 或只使用数值列，所以结论仍然可用。特征工程应改回“读一次、显式指定类型”。

------------------------------------------------------------------------

## 7. 描述统计（无图）

### 7.1 为什么先做表、不先做图

直方图的箱数、箱线图要不要隐藏极端点、时间序列要不要从 0 起画，都取决于偏态、极差和结构零有多严重。所以先算均值、中位数、方差、四分位数，再选图。

### 7.2 方法

对全部记录和 `Open = 1` 分别计算 `Sales` 与 `Customers`。缺失值先丢掉。本数据这两列没有缺失，这一步是保护性写法。

公式：

$$
\bar{x} = \frac{1}{n}\sum_{i=1}^{n} x_i
$$

中位数：奇数样本取排序后中间一个；偶数样本取中间两个的平均。

样本方差，Pandas 默认 `ddof=1`：

$$
s^2 = \frac{\sum (x_i-\bar{x})^2}{n-1}
$$

$$
s = \sqrt{s^2}
$$

$$
IQR = Q_3 - Q_1 = P_{75} - P_{25}
$$

分位数用 Pandas `quantile`，没有改成另一种分位数算法。样本方差用 \(n-1\)，因为这是样本而不是对全体零售日的普查总体，和 Pandas 默认一致，便于审计。

另外对营业日 Sales 用总和和离差平方和把均值、方差手算一遍，并与 `mean()`、`var(ddof=1)`、`std(ddof=1)` 做 `np.isclose`。四项全部为 True。

### 7.3 结果

全部记录 Sales：均值 5,773.82，中位数 5,744.00。

营业日 Sales：

- \(n = 844{,}392\)
- 总和 = 5,873,180,623
- 均值 = 5,873,180,623 / 844,392 = 6,955.51
- 中位数 = 6,369.00
- 均值比中位数高 586.51
- 方差 = 9,636,148.78
- 标准差 = 3,104.21
- Q1 = 4,859，Q3 = 8,360，IQR = 3,501
- 最大值 = 41,551

全部记录 Customers：均值 633.15，中位数 609.00。

营业日 Customers：均值 762.73，中位数 676，Q1 = 519，Q3 = 893，IQR = 374，标准差 401.23，最大值 7,388。

结构零把全部记录的销售均值压低了 1,181.69（6,955.51 − 5,773.82）。所以“日历日平均”和“营业日平均”不能混用。

选图结论：连续、右偏、长尾，适合直方图加均值/中位数线，再用箱线图标出常规上界。不做对数轴，也不截断，因为第一张图的任务是保留真实形态。

------------------------------------------------------------------------

## 8. 全部图：选择原因、参数和结果

共 15 张已保存的图。下面按分析顺序写。路径均相对于项目根目录。

### 8.1 `outputs/figures/sales_distribution_open.png`

分析问题：营业日销售额集中在哪里，是否对称，高销售尾部有多长。

为什么用直方图，而不是密度曲线或箱线图：`Sales` 是连续数值，这一步要看经验频数，而不是只看五个摘要点。直方图按区间计数，能同时看见峰、偏态和尾部。密度曲线会平滑掉频数的绝对规模；箱线图留到下一张，用来量化 IQR 上界。

方法：

$$
f_j = \sum_{i=1}^{n} I(x_i \in B_j)
$$

保留全部观测，不做对数变换，不截断横轴。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| 数据 | `Open = 1` 的 `Sales`，844,392 条 | 去掉结构零 |
| 图类型 | `ax.hist` | 看频数分布 |
| `bins` | 70 | 样本量很大，箱太少会抹平右尾，箱太多会让柱碎裂。70 在可读和细节之间折中 |
| `color` | `#AFCBFF` | 项目主浅蓝色，低饱和，不抢注释 |
| `edgecolor` | white | 让相邻柱边界可辨，又不用深色描边加重柱子 |
| `linewidth` | 0.45 | 白边够细，不会占掉柱宽 |
| `alpha` | 0.90 | 略透明，和网格共存时柱身仍清楚 |
| `figsize` | (9.5, 5.5) | 比默认 (8, 5) 略宽，给右尾和最大值注释留空 |
| 显示 dpi | 沿用默认 120 | 笔记本内预览 |
| 保存 dpi | 300，`bbox_inches="tight"` | 报告分辨率 |
| 均值线 | `#4A4A4A`，虚线，线宽 1.8 | 虚线表示计算出来的参照，不是数据本身 |
| 中位数线 | `#222222`，实线，线宽 1.8 | 实线更醒目，用来和均值对比偏态 |
| 最大值 | 倒三角 `v`，`s=70`，填充 `#F7D6A3`，边 `#4A4A4A`，线宽 0.8 | 最大值在横轴远端，单独标记避免被看成断轴 |
| 标记高度 | 直方图峰值 × 0.025 | 贴在底部，不挡住主峰 |
| 注释偏移 | `xytext=(-15, 32)`，右对齐 | 最大值在右端，注释向左放，避免出图 |
| 横轴 | 0 到 `max × 1.04` | 从 0 开始，避免截断造成“看起来像从几千起跳”；右侧留 4% 给标记 |
| y 轴格式 | `style="plain"` | 不用科学计数法，频数直接可读 |
| 网格 | 关 x，y 的 alpha 0.55 | 频数读的是垂直高度，垂直网格更有用；水平网格会打断柱 |

结果：单峰、右偏，均值在中位数右侧，最大值 41,551 远离主体。与描述统计一致。高销售记录保留。

### 8.2 `outputs/figures/sales_boxplot_open.png`

分析问题：常规统计上界以外有多少点，主体的一半数据落在哪个区间。

为什么在直方图之后再用箱线图：直方图负责形状，箱线图负责 Q1、中位数、Q3 和 Tukey 上界。两张图回答的不是同一个问题。

方法：

$$
Upper = Q_3 + 1.5 \times IQR = 8{,}360 + 1.5 \times 3{,}501 = 13{,}611.5
$$

下界 \(Q_1 - 1.5 \times IQR\) 会低于 0。销售额不能为负，所以图上标的是上界，离群点定义为大于 13,611.5 的营业日销售。

为什么不直接用 `showfliers=True`：极端点很多，默认飞点会堆成一条深带，盖住箱体。所以箱线图关闭飞点，再单独用低透明度散点画离群点。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| 方向 | `vert=False` | 水平箱线图把销售额留给较长的横轴，便于标 Q1、Q3 和上界 |
| `patch_artist` | True | 箱体可以填浅色，而不只是空框 |
| `widths` | 0.45 | 单箱不要占满整个纵轴 |
| `showfliers` | False | 飞点改由散点控制透明度 |
| 箱体 | 填充 `#D8C7F0`，边 `#555555`，线宽 1.2 | 浅紫色和直方图蓝色区分，表示这是第二张摘要图 |
| 中位数线 | `#222222`，线宽 2 | 比箱边更粗，避免和 Q1/Q3 边混淆 |
| 须和端线 | `#555555`，线宽 1.2 | 和箱边同一层级 |
| 离群点 | `s=10`，`#E8A6A6`，`alpha=0.15`，无边 | 3 万多个点若用实心深色会糊成一块；浅红、高透明只表达“右尾很密” |
| 上界线 | 虚线，`#777777`，线宽 1.2 | 这是规则边界，不是观测值 |
| `figsize` | (14, 5) | 水平方向要放下从 0 到 4 万的轴和注释 |
| dpi | 图窗 300，保存也是 300 | 清晰，但像素会偏大 |
| y 刻度 | 空 | 只有一个箱，纵轴类别没有信息 |
| 网格 | 仅 x，alpha 0.15 | 水平箱线图读的是横向位置 |

结果：上界 13,611.50；离群点 30,769 条，占营业日 3.64%。与检查阶段的 IQR 筛查一致。这些点保留。

实现备注：Matplotlib 3.11 已弃用 `vert`，警告要求改成 `orientation="horizontal"`。功能仍正确，下次改图时替换即可。

### 8.3 `outputs/figures/daily_average_sales_trend_ma7.png`

分析问题：2013 至 2015 年中，单店营业日销售有没有趋势、周期和尖峰。

为什么不用每日总销售额折线：每天开门的门店数不同。元旦这类日子只有十几家店开门，总销售额会很低，但这不代表开门店铺卖得差。所以按日计算：

$$
\bar{S}_t = \frac{\sum_{i=1}^{n_t} Sales_{i,t}}{n_t}
$$

其中 \(n_t\) 是当天营业记录数。代码里用 `groupby("Date")` 后对 `Sales` 取 `mean`，对 `Store` 取 `count`。在这个数据里业务键唯一，`count` 等于当天营业门店数。

为什么是折线：横轴是连续日期，要看的是变化路径，不是类别对比。柱状图 900 多天会挤在一起，也读不出趋势。

为什么又加 7 日移动平均：原始日序列波动大，周内节奏会盖住较慢的变化。Markdown 第 5.1 节曾写“不平滑”，最终实现改成了日线加 7 日均线，并存成 `daily_average_sales_trend_ma7.png`。没有另存不带均线的 `daily_average_sales_trend.png`。以代码和已保存文件为准。

$$
MA7_t = \frac{AverageSales_{t-6}+\cdots+AverageSales_t}{7}
$$

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| 窗口 | `rolling(window=7, min_periods=1)` | 7 天对应一个零售周；序列开头不足 7 天时用已有天数，避免前 6 天空白 |
| 排序 | 按 `Date` 排序后再 rolling | 滚动窗口必须按时间顺序 |
| 日线 | `#B7D4F5`，线宽 1，alpha 0.35 | 浅、细、透明，作为背景波动 |
| 均线 | `#CDB4DB`，线宽 2.5 | 更粗、更深的浅紫，作为要阅读的趋势 |
| `figsize` | (14, 5) | 两年半的横轴需要宽图，否则周末尖峰粘在一起 |
| 保存 dpi | 300，`bbox_inches="tight"` | 报告用 |
| y 轴 | 不从 0 起 | 日均销售大致在同一量级，从 0 起会把波动压成一条几乎水平的带 |
| y 边距 | \((\max-\min)\times 0.08\) | 上下各留 8%，避免曲线贴边，又不会留出大块空白 |
| 网格 | alpha 0.25 | 时间图上网格只作参照 |
| 刻度 | 旋转 45°，右对齐 | 日期标签长，水平放会重叠 |

开头几天的计算结果可用于核对：2013-01-01 只有 17 家店开门，日均 5,719.71；01-02 有 1,111 家，日均 6,255.47。这正说明必须按营业店平均，而不能画每日总和。

建模含义：存在可见的时间波动，验证集不能随机打乱日期。训练集结束于 2015-07-31，测试集从 2015-08-01 开始，应按时间切分。

### 8.4 `outputs/figures/weekday_sales_boxplot.png`

分析问题：星期几是否有稳定的销售差异。

为什么用分组箱线图，而不是只画七个均值柱：要同时看中位数、IQR 和分布重叠。只画均值会夸大星期一和星期六的差距，也会看不见星期日极宽的散布。均值仍用黑点叠加上去，因为箱线图的中位数不等于均值，右偏时两者会分开。

方法：只保留营业日，把 Rossmann 的 `DayOfWeek` 映射为 Mon 到 Sun。这个数据集里 1 是星期一，7 是星期日，不是 Python 的 Monday=0。

$$
\bar{x}_d = \frac{1}{n_d}\sum_{i=1}^{n_d} x_i
$$

每个箱的中位数、Q1、Q3、IQR 由数据计算，不另设阈值。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| `figsize` | (11, 6)，dpi 300 | 七个箱需要横向空间 |
| `tick_labels` | Mon 到 Sun | 固定业务顺序，不用字母排序 |
| `showfliers` | False | 极端高销售会把箱体压到图的底部，星期差异就看不见了 |
| `widths` | 0.6 | 箱之间留缝，便于比较 |
| 中位数线 | `#333333`，线宽 1.8 | 跨组比较时中位数必须明显 |
| 须和箱边 | `#777777`，线宽 1.2 | 次于中位数 |
| 箱体颜色 | `#BFD7F6`, `#D8C6F0`, `#F5C6D6`, `#C9E7D8`, `#F7E3A1`, `#F6C8A8`, `#F3B6B6` | 七天七色，避免读者把相邻箱看成同一组 |
| 箱体 alpha | 0.75 | 浅色叠加后仍能看见网格 |
| 均值点 | `#333333`，`s=45` | 和中位数同色系，但用点而不是线，两种摘要不会被看成同一种统计量 |
| 网格 | 仅 y，alpha 0.2 | 比较的是各组销售额高度 |

结果：

| 星期 | 条数 | 均值 | 中位数 | Q1 | Q3 | IQR |
|---|---:|---:|---:|---:|---:|---:|
| 一 | 137,560 | 8,216.07 | 7,539 | 5,538 | 10,133 | 4,595 |
| 二 | 143,961 | 7,088.11 | 6,502 | 4,960 | 8,521 | 3,561 |
| 三 | 141,936 | 6,728.12 | 6,210 | 4,829 | 7,987 | 3,158 |
| 四 | 134,644 | 6,767.31 | 6,246 | 4,900 | 7,987 | 3,087 |
| 五 | 138,640 | 7,072.68 | 6,580 | 5,205 | 8,324 | 3,119 |
| 六 | 144,058 | 5,874.84 | 5,425 | 3,925 | 7,232 | 3,307 |
| 日 | 3,593 | 8,224.72 | 6,876 | 3,314 | 11,418 | 8,104 |

星期一较高，星期六较低。星期日条数不到平常日的 3%，IQR 最大，均值被高销售尾巴拉高，中位数和均值差得更多。星期之间分布重叠很大，所以 `DayOfWeek` 应作为特征，但不能指望它单独解释销售。

### 8.5 `outputs/figures/customers_sales_scatter.png`

分析问题：客流和销售额是不是同向变化，关系大致是否线性。

为什么用散点图：相关矩阵只给一个数。散点图能看出是直线、弯曲、扇形方差还是少数点造成的高相关。

相关在全部营业日上计算，图只抽样。原因是 84 万个点叠在一起会变成实心色块，读不出密度。相关不能用样本代替，否则报告里的相关系数会随抽样变化。

Pearson：

$$
r = \frac{\sum (x_i-\bar{x})(y_i-\bar{y})}{\sqrt{\sum (x_i-\bar{x})^2}\sqrt{\sum (y_i-\bar{y})^2}}
$$

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| 相关 | `corr(method="pearson")`，全量营业日 | 量化线性关系 |
| 抽样 | `n=min(30000, len)`，`random_state=42` | 3 万点足够看形状；固定种子保证图可复现 |
| 散点 | `#B79AD8`，`s=8`，`alpha=0.15` | 点小且透明，重叠处颜色变深，用颜色深浅代替频数 |
| 趋势线 | `np.polyfit(..., 1)`，再 `poly1d` | 一次多项式就是直线参照，用来看是否大体线性 |
| 趋势线拟合对象 | 抽样点，不是全量 | 与图中可见点一致；斜率不是全量回归系数，不能当模型系数引用 |
| 趋势线横轴 | 样本最小到最大，200 个等距点 | 线要平滑，不必每个样本一个点 |
| 趋势线样式 | `#1F77B4`，线宽 2.5 | 比散点更深，保证浅色点上方仍能看见趋势 |
| `figsize` | (8, 6)，dpi 300 | 散点需要接近方形的作图区，避免横纵比例把斜率看歪 |
| 图例框 | `frameon=True` | 这张图背景有大量点，无框图例会混进散点 |
| 网格 | alpha 0.2 | 弱网格，帮助读数值 |

结果：Pearson \(r = 0.8236\)。客流越高，销售额通常越高，图上的抽样趋势线向上。

建模限制：测试集没有 `Customers`。同一天的客流不能作为预测输入，否则是在使用预测时拿不到的信息。EDA 里写“可作为神经网络输入”这一点，不能直接带到 `05_model.ipynb`。可以使用的是预测日之前已经发生的历史客流，例如门店历史均值或滞后值。当天客流只能作为机制解释。

### 8.6 `outputs/figures/sales_promotion_boxplot.png`

分析问题：`Promo = 1` 的日子，销售额分布是否整体更高，而不只是均值被少数高点拉动。

为什么用箱线图：促销是二元条件。箱线图能比较两组的中位数和中间 50% 区间。若只比均值，无法判断是整体上移还是右尾拉动。

范围仍是营业日。关门日销售为 0，放进去会把“没开门”算成“促销无效”。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| 分组 | `Promo == 0` 与 `Promo == 1` | 数据里促销是 0/1，不是连续强度 |
| 统计 | count, mean, median, std | 同时看样本量、位置和离散 |
| `showfliers` | False | 和星期箱线图同一原因，避免极端点压缩箱体 |
| 颜色 | 无促销 `#A8C7E8`，促销 `#F6C58A` | 蓝/橙是项目里反复使用的二元对比色，便于和后面的客流图对照 |
| `figsize` | (8, 6)，dpi 300 | 两组对比不需要很宽 |
| 网格 | 仅 y，alpha 0.2 | 比较高度 |
| `patch_artist` | True | 才能填组颜色 |

结果：

| Promo | 条数 | 销售均值 | 中位数 | 标准差 |
|---:|---:|---:|---:|---:|
| 0 | 467,496 | 5,929.41 | 5,459 | 2,629.65 |
| 1 | 376,896 | 8,228.28 | 7,649 | 3,175.76 |

均值大约高 2,299，中位数大约高 2,190。中位数和均值一起上移，说明不是少数异常点造成的。`Promo` 在测试集里存在，应作为核心特征。

这张图是描述性对比，不是因果实验。促销日可能和其他日历因素重合。建模时要和星期、节假日一起放入，不能把 2,299 直接当成促销的净效应。

### 8.7 `outputs/figures/sales_stateholiday_boxplot.png`

分析问题：还在营业的门店，在不同州假类别上销售分布是否不同。

为什么先用箱线图：类别有四个，而且假日样本量很小。箱线图能暴露中位数、散布，以及小样本组是否不稳定。只看平均柱状图会把小样本的高均值说得过满。

类别映射：

- `0` → No Holiday
- `a` → Public Holiday A
- `b` → Public Holiday B
- `c` → Public Holiday C

映射前先 `astype(str)`。清洗后文件已经统一，这里再次统一是为了单元可单独重跑。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| `showfliers` | False | 小样本组的极端点会主导纵轴 |
| `figsize` | (9, 6)，dpi 300 | 四个长标签需要比二元图更宽 |
| 颜色 | `#B8D8F0`, `#F6C58A`, `#C7E6A3`, `#D7BDE2` | 四组浅色区分 |
| `xticks` 旋转 | 20° | 标签较长，略旋转即可，不必 45° |
| 网格 | 仅 y，alpha 0.2 | 比较销售额高度 |
| 类别顺序 | 这一张使用 `groupby` 后的索引顺序 | 输出表顺序是 No Holiday, A, B, C |

结果：

| 类别 | 条数 | 均值 | 中位数 | 标准差 |
|---|---:|---:|---:|---:|
| No Holiday | 843,482 | 6,953.52 | 6,368 | 3,098.87 |
| Public Holiday A | 694 | 8,487.47 | 7,556 | 5,708.63 |
| Public Holiday B | 145 | 9,887.89 | 8,423 | 7,551.78 |
| Public Holiday C | 71 | 9,743.75 | 8,397 | 5,980.86 |

假日营业日的均值更高，但 B、C 只有几十到一百多条，标准差也大得多。多数假日门店是关门的，这张图只描述“假日仍然开门”的门店，不能外推成“假日会让所有门店销售额上升”。建模时 `StateHoliday` 必须和 `Open` 一起用。

### 8.8 `outputs/figures/average_sales_stateholiday.png`

分析问题：四个类别的平均销售额谁高谁低，数字能否直接读出来。

为什么在箱线图之外再画柱状图：`StateHoliday` 是类别变量，柱状图适合比较一个摘要量。箱线图已经回答分布问题；柱状图只比较均值，并在柱顶标数字，避免读者从坐标轴估读。两张图不是重复，而是把“分布”和“平均水平”拆开。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| 聚合 | `groupby` 后 `mean` | 柱高就是平均销售额 |
| 顺序 | 显式 `reindex`：No Holiday, A, B, C | 不依赖字母序，避免和箱线图读序不一致 |
| `figsize` | (9, 6)，dpi 300 | 与箱线图同宽，方便对照 |
| `width` | 0.6 | 柱之间留空，四个类别不会连成一片 |
| 颜色 | `#A8C7E8`, `#D7BDE2`, `#F6C58A`, `#C7E6A3` | 与箱线图同属浅色系统，但顺序不完全相同，读图时应以标签为准，不能只记颜色 |
| 数值标签 | `f"{value:.0f}"`，水平居中，`va="bottom"` | 均值是整数读法就够，标在柱顶 |
| 刻度旋转 | 20° | 与箱线图一致 |
| 网格 | 仅 y，alpha 0.2 | 柱状图的 x 网格没有信息 |

不要把柱高理解成因果效应。Public Holiday B 的平均柱最高，但只有 145 条营业记录。

### 8.9 `outputs/figures/sales_schoolholiday_boxplot.png`

分析问题：学校假期是否改变销售分布。

为什么用带均值标记的箱线图：学校假期预期影响小于公共假日和促销。如果只比均值，可能把很小的差异说成“有明显提升”。箱线图能看中间 50% 是否真的分开；菱形均值用来显示右偏下均值和中位数的差别。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| 组 | `SchoolHoliday` 0 和 1 | 二元日历变量 |
| 统计 | count, mean, median, std | 与促销图同一套摘要 |
| `showfliers` | False | 保持箱体可读 |
| 箱色 | `#A8C7E8`, `#D7BDE2` | 蓝/紫，表示这是假期对比而不是促销橙 |
| 均值 | 菱形 `D`，`s=60`，`#E76F51` | 暖色菱形和浅色箱体分开，专门表示均值 |
| `figsize` | (8, 6)，dpi 300 | 二元对比 |
| 网格 | 仅 y，alpha 0.2 | 比较高度 |

结果：

| SchoolHoliday | 条数 | 均值 | 中位数 | 标准差 |
|---:|---:|---:|---:|---:|
| 0 | 680,935 | 6,896.78 | 6,326 | 3,083.77 |
| 1 | 163,457 | 7,200.18 | 6,562 | 3,176.30 |

均值只高约 303，中位数高约 236。分布重叠很大。学校假期应保留为特征，但优先级低于 `Promo` 和星期。测试集有这个字段。

### 8.10 `outputs/figures/average_sales_storetype.png`

分析问题：门店类型是否有稳定的销售水平差异。

数据先按 `Store` 左连接 `store_clean`。检查阶段已确认门店集合完全相同，左连接不会产生未匹配门店。仍只保留营业日。

为什么用柱状图：`StoreType` 是类别，要比较的是每类平均销售额。Markdown 还写了同时展示门店数量。代码只实现了平均销售额柱状图，没有单独的门店数图。门店数写在统计表里，不在图里。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| 聚合 | 组内 `Sales` 均值、中位数，`Store` 的 `nunique`，观测条数 | 均值不能脱离门店数量解释 |
| 排序 | `Average_Sales` 降序 | 读图时最高的类型在左，不按字母排序 |
| `figsize` | (8, 6)，dpi 300 | 四个类别 |
| 颜色 | `#B8D8F0`, `#D7BDE2`, `#F6C58A`, `#C7E6A3` | 按排序后的位置涂色，不是按类型字母固定颜色 |
| 数值标签 | 四舍五入到整数，柱顶居中 | 直接读出平均销售额 |
| 网格 | 仅 y，alpha 0.2 | 比较柱高 |

结果：

| StoreType | 平均销售 | 中位数 | 门店数 | 营业日条数 |
|---|---:|---:|---:|---:|
| b | 10,231.41 | 9,130 | 17 | 15,563 |
| c | 6,932.51 | 6,407 | 148 | 112,978 |
| a | 6,925.17 | 6,285 | 602 | 457,077 |
| d | 6,822.14 | 6,395 | 348 | 258,774 |

类型 b 明显更高，但只有 17 家店。a、c、d 的均值很接近。不能因为 b 的均值高就把它当成所有门店的主要区分因素，也不能因为样本少就删掉这个类别。编码时应保留 b，并意识到它是稀有高水平组。

颜色按排序位置分配，所以这张图的第一个浅蓝色柱是类型 b，不是类型 a。以后若重画，建议颜色按类型固定，避免和别的图对照时看错。

### 8.11 `outputs/figures/store_distribution_assortment.png`

分析问题：品类策略 `Assortment` 的门店结构是否均衡。

为什么单独画门店数，而不和销售额画在同一张双轴图上：门店数大约是几百，平均销售额是几千，双轴会强迫两个量共用一个图，也容易被读成因果。项目要求是数据表达优先，所以拆成两张柱状图。这一张只回答“样本结构”。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| 高度 | `Number_of_Stores`，即组内 `Store` 的 `nunique` | 数的是门店，不是日记录 |
| `figsize` | (7, 5)，dpi 300 | 三个类别，略窄于四组图 |
| 颜色 | `#A8C7E8`, `#D7BDE2`, `#F6C58A` | 按 `groupby` 的 a、b、c 顺序 |
| 标签 | 整数门店数，柱顶 | 结构图要能直接读数 |
| 网格 | 仅 y，alpha 0.2 | 比较柱高 |

结果：a 有 593 家，c 有 513 家，b 只有 9 家。和 StoreType b 一样，Assortment b 是稀有组。

### 8.12 `outputs/figures/average_sales_assortment.png`

分析问题：品类策略是否对应不同的营业日销售水平。

为什么用第二张柱状图：在已经看过门店数之后，再单独比较平均表现，避免把“店多”读成“卖得好”。

参数与上一张相同：`figsize=(7, 5)`，dpi 300，同一套三色，柱顶标注，y 网格 alpha 0.2。柱高换成 `Average_Sales`，标签四舍五入到整数。颜色顺序仍按 a、b、c，因此浅紫色对应稀有的 b。

结果：

| Assortment | 门店数 | 平均销售 | 中位数 | 营业日条数 |
|---|---:|---:|---:|---:|
| a | 593 | 6,621.02 | 6,082 | 444,909 |
| b | 9 | 8,639.35 | 8,081 | 8,212 |
| c | 513 | 7,300.53 | 6,675 | 391,271 |

c 的平均销售高于 a，而且门店数足够多，这个差异比 StoreType a/c/d 之间的差异更值得保留。b 更高但只有 9 家店。`Assortment` 应作为类别特征进入模型。

### 8.13 `outputs/figures/competition_distance_distribution.png`

分析问题：最近竞争店距离本身是什么形状，适不适合直接当线性输入。

为什么用直方图：距离是连续、强右偏的数值。在画相关散点之前，要先知道大多数店距离很近、少数店非常远。若不看分布，散点图右侧少数远距离点会主导视觉。

范围：合并后的营业日，去掉 `CompetitionDistance` 缺失。描述统计基于 842,206 条日记录，不是 1,112 家有距离的门店。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| `bins` | 60 | 距离跨度从 20 到 75,860，60 箱能看见近距离高峰，又不至于碎 |
| `figsize` | (8, 6)，dpi 300 | 与相关散点对照 |
| 颜色 | 未指定，使用 Matplotlib 默认色 | 这是风格不一致。没有使用项目浅色。重画时应改成 `#AFCBFF` 并加白边 |
| 网格 | alpha 0.2 | 弱参照 |
| y 轴标签 | `"Number of Stores"` | 标签不准确。直方图画的是日记录条数，不是门店数。同一门店会被重复计数 |

结果：均值 5,457.98，标准差 7,809.44，最小 20，Q1 710，中位数 2,320，Q3 6,890，最大 75,860。均值远高于中位数，右偏很强。若做线性特征，应考虑分箱、对数，或至少不要把原始距离的线性项当成主要信号。3 家缺失距离的门店不在这张图里，建模时要单独做缺失指示。

### 8.14 `outputs/figures/competition_distance_sales_scatter.png`

分析问题：距离和日销售额有没有可见的线性关系。

为什么用散点加直线：相关是 −0.04 量级时，必须看图，确认“接近 0”不是弯曲关系被线性相关抵消，也不是坐标尺度造成的错觉。

相关在去掉缺失距离的全部营业日上计算。图仍抽样，原因与客流散点相同。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| 相关 | 全量非缺失营业日，Pearson | 报告用的是这个数，不是抽样相关 |
| 抽样 | 30,000，`random_state=42` | 与客流图同一套可复现抽样规则 |
| 散点 | `alpha=0.15`，`s=8`，颜色未指定 | 透明度是为了重叠可读；颜色未使用浅紫，和客流图不一致 |
| 趋势线 | 对抽样点做一次 `polyfit`，200 个等距点，`#1F77B4`，线宽 2.5 | 直线参照；斜率属于抽样拟合，不应写成总体回归系数 |
| `figsize` | (8, 6)，dpi 300 | 与上一张对照 |
| 网格 | alpha 0.2 | 弱网格 |

结果：Pearson \(r = -0.0364\)。图上的趋势几乎是平的。更远并不稳定地对应更高或更低的日销售。原始距离不适合作为强线性特征。更值得在特征阶段尝试的是：缺失指示、距离分箱、竞争店是否已开业、开业后经过的月数。后两项 EDA 还没有做，因为开业日期有 354 家缺失，不能先填一个假日期再算时长。

### 8.15 `outputs/figures/customers_promotion_boxplot.png`

分析问题：促销日销售额更高，是因为来的人更多，还是只因为客单价更高。

数据没有直接的客单价。可观察的中间变量是客流。如果促销日客流分布整体上移，说明至少有一部分销售提升来自进店人数；如果客流不变而销售额上升，则更像是购买强度变化。两者可以同时存在。这张图不能分离净效应，但能排除“客流完全没变”这一种说法。

为什么用带均值菱形的箱线图：和学校假期图同一设计。要看的是分布位置，而不只是均值。颜色沿用促销销售图的蓝/橙，表示这是同一组条件换了因变量。

参数：

| 参数 | 取值 | 原因 |
|---|---|---|
| 因变量 | 营业日 `Customers` | 关门日客流为 0，会歪曲对比 |
| 分组 | `Promo` 0/1 | 与销售促销图一致，便于并读 |
| `showfliers` | False | 避免极端客流压缩箱体 |
| 箱色 | `#A8C7E8`, `#F6C58A` | 与 `sales_promotion_boxplot.png` 相同，方便两张图对照 |
| 均值 | 菱形，`s=60`，`#E76F51` | 与学校假期图相同的均值标记规则 |
| `figsize` | (8, 6)，dpi 300 | 二元对比 |
| 网格 | 仅 y，alpha 0.2 | 比较客流高度 |

结果：

| Promo | 条数 | 客流均值 | 中位数 | 标准差 |
|---:|---:|---:|---:|---:|
| 0 | 467,496 | 696.86 | 610 | 388.91 |
| 1 | 376,896 | 844.43 | 757 | 401.24 |

促销日客流均值大约高 148，中位数大约高 147。销售和客流一起上升，因此促销不是单纯的“来店人数不变、只提高客单价”。当天客流仍然不能作为测试集特征。这张图的用途是理解机制，以及支持“历史客流水平可能携带门店需求信息”。

------------------------------------------------------------------------

## 9. 图总表

| 文件 | 图类型 | 分析问题 | 关键参数 |
|---|---|---|---|
| `sales_distribution_open.png` | 直方图 | 营业日销售的形状和右尾 | bins=70，浅蓝色，均值虚线，中位数实线，横轴 0 到 max×1.04，保存 dpi 300 |
| `sales_boxplot_open.png` | 水平箱线图 | IQR 与高值离群 | 1.5 IQR 上界 13611.5，飞点关闭后用 alpha 0.15 散点重画，`vert=False` |
| `daily_average_sales_trend_ma7.png` | 折线 | 去开门数量影响后的时间趋势 | 日均按营业店，MA7，`min_periods=1`，y 轴不从 0 起，边距 8% |
| `weekday_sales_boxplot.png` | 分组箱线图 | 星期规律 | 1=周一至 7=周日，隐藏飞点，黑色均值点 |
| `customers_sales_scatter.png` | 散点 + 直线 | 客流与销售的关系 | 全量 Pearson，图抽样 30000，`random_state=42`，点 alpha 0.15 |
| `sales_promotion_boxplot.png` | 箱线图 | 促销是否提高销售 | 营业日，隐藏飞点，蓝/橙 |
| `sales_stateholiday_boxplot.png` | 箱线图 | 州假类别的销售分布 | 小样本假日，隐藏飞点，标签旋转 20° |
| `average_sales_stateholiday.png` | 柱状图 | 州假类别的平均水平 | 固定类别顺序，柱宽 0.6，柱顶整数标签 |
| `sales_schoolholiday_boxplot.png` | 箱线图 + 均值菱形 | 学校假期差异 | 隐藏飞点，均值 `#E76F51` |
| `average_sales_storetype.png` | 柱状图 | 门店类型平均销售 | 按均值降序，颜色随位置而不是随类型固定 |
| `store_distribution_assortment.png` | 柱状图 | 品类的门店结构 | `nunique(Store)`，三色按 a/b/c |
| `average_sales_assortment.png` | 柱状图 | 品类的销售水平 | 与结构图拆开，避免双轴误读 |
| `competition_distance_distribution.png` | 直方图 | 竞争距离的分布 | bins=60；y 轴标签写成了门店数，实际是日记录 |
| `competition_distance_sales_scatter.png` | 散点 + 直线 | 距离与销售是否线性相关 | 全量相关，图抽样 30000，种子 42 |
| `customers_promotion_boxplot.png` | 箱线图 + 均值菱形 | 促销是否同时提高客流 | 与销售促销图使用同一蓝/橙 |

没有保存、但 Markdown 曾提到的文件：

- `daily_average_sales_trend.png`：被 7 日均线版本取代。
- StoreType 的门店数量图：只做了统计表，没有单独出图。

------------------------------------------------------------------------

## 10. 已确认的建模含义

这些结论应直接约束下一阶段，而不是再做一轮无目标的画图。

1. 预测粒度应对齐测试集：门店 × 日，2015-08-01 至 2015-09-17。`04_features.ipynb` 里的“月度建模”占位描述与提交文件不一致。除非课程要求改目标，默认做日粒度特征表。
2. 切分必须按时间。训练集止于 2015-07-31。随机打乱会把未来信息泄漏到训练中。
3. 若 `Open` 已知为 0，销售额应为 0。测试集有 11 个 `Open` 缺失，需要单独规则，不能默默删掉。
4. `Promo` 是最强的运营特征之一，测试集可用。它同时抬高销售和客流。
5. 星期要进入模型。星期一高、星期六低。星期日样本少、方差大。
6. `StateHoliday` 要保留，但不能把“假日仍开门的高均值”当成全体门店的假日效应。
7. `SchoolHoliday` 保留，效应小于促销和星期。
8. `StoreType` 和 `Assortment` 保留。b 类稀有但水平高，不能删除。
9. 同一天的 `Customers` 禁止作为测试期输入。可用的是预测日之前已知的历史客流。
10. `CompetitionDistance` 与日销售几乎无线性相关，且强右偏。优先考虑分箱和缺失指示，而不是一个原始线性项。
11. Promo2 细节缺失表示“不适用”。竞争开业日期缺失表示“未知”。两类都不能用均值或 0 填掉。
12. 目标右偏，最大销售 41,551，3.64% 的营业日超过 13,611.5。不删除尾巴。可考虑对数目标、或在评价时同时看绝对误差和相对误差。
13. 54 条营业零销售保留。

尚未在 EDA 中展开、但清洗时已保留的字段：`Promo2`、`Promo2SinceWeek`、`Promo2SinceYear`、`PromoInterval`、`CompetitionOpenSinceMonth`、`CompetitionOpenSinceYear`。它们应在特征工程里显式编码，而不是再做一轮盲目填补。

------------------------------------------------------------------------

## 11. 下一步

按当前仓库状态，下一步是 `notebooks/04_features.ipynb`，不是再扩展 EDA。

建议顺序：

1. 写明预测粒度是日、门店级，并说明为什么不采用占位注释里的月度聚合。
2. 以 `train_clean` 左连接 `store_clean`，一次读取并显式指定类型。
3. 构造时间特征：星期、月份、年中周数，以及训练截止日之前的滞后销售。滞后只能使用预测日之前的信息。
4. 构造促销、州假、学校假期、门店类型、品类特征。稀有类别保留。
5. 对 Promo2 做“是否参加”和“不适用”编码，不填开始周和开始年。
6. 对竞争信息做缺失指示。距离若使用，先分箱或变换，不直接依赖已证实很弱的线性关系。
7. 不把当天 `Customers` 放进特征矩阵。
8. 进入 `05_model.ipynb` 前先确定时间验证窗口。一个合理验证窗是贴近测试期的最后若干周，例如 2015 年 6–7 月，训练只用更早的数据。
9. 先做基线，再做神经网络。基线可以是“关门为 0，营业日用门店历史中位数或促销分组中位数”。没有基线时，神经网络的误差无法解释。
10. 选定神经网络库后再写入 `requirements.txt`。当前依赖里还没有它。

EDA 本身可以视为完成。若要重画图，只需处理已记录的实现细节：弃用的 `vert`、竞争距离直方图的 y 轴标签和默认颜色、以及部分单元重复读入时的类型警告。这些不影响上述结论。

------------------------------------------------------------------------

## 12. 最终阶段记录（2026-09-17）

本节补在已有过程之后。前面“特征工程尚未开始”“建模仅有计划注释”的文字是当时状态，保留不删。

### 12.1 特征工程

`notebooks/04_features.ipynb` 已完成。

- 预测粒度是门店日销售，与官方测试窗口一致。
- 当天 `Customers` 不进入模型输入。保留 `Customers_lag_1`、`Customers_lag_7`、`Customers_rolling_7`，以及销售滞后和滚动特征。
- 滚动特征在合并后的 Store–Date 时间线上生成，再拆回训练表和测试表，使两边特征列一致。
- 导出：`data/processed/train_feature_final.csv`（1,017,209 × 40）和 `data/processed/test_feature_final.csv`（41,088 × 39）。
- 随机森林只用于解释特征，不是最终预测模型。已打印的重要性：`Open` 0.472434，`Sales_lag_14` 0.287071。历史需求分组贡献 89.35%。
- 图在 `outputs/figures/04_feature_engineering/`。

### 12.2 最终模型

保留的模型文件是 `outputs/models/final_sales_model.pkl`。这次整理没有重新训练，也没有覆盖该文件。

```text
MLPRegressor
隐藏层：128 → 64 → 32
activation: relu
solver: adam
learning_rate_init: 0.001
max_iter: 50
early_stopping: False
random_state: 42
输入特征数：49
```

`notebooks/05_model.ipynb` 现在按这个架构记录，并读取已保存文件做一致性检查。该单元不调用 `fit()`。

`outputs/figures/05_model/` 中的探索图保留，包括 baseline、log target、architecture comparison 和 early stopping。这些柱状图在 notebook 里没有打印精确柱高，所以这里不抄写成正式指标。最终保留的是已保存的 128 → 64 → 32 网络。选择依据是验证集表现，打印数字在 notebook 06。

配套预处理器是 `outputs/models/preprocessor.pkl`，只在训练时间段上拟合。

### 12.3 评估

`notebooks/06_evaluation.ipynb` 在 2015 年 7 月验证窗口上评估，34,565 行，编码后 49 个特征。已知 `Open = 0` 的预测在推断后置为 0。

- MAE：491.243061
- RMSE：731.555819
- R²：0.958850
- 平均残差（实际 − 预测）：27.449545
- 残差标准差：731.040655
- 按销售额分档的平均绝对误差：Low 247.110986，Medium 481.630176，High 745.055776

图：

- `outputs/figures/06_evaluation/06_final_actual_vs_predicted.png`
- `outputs/figures/06_evaluation/06_final_residual_distribution.png`
- `outputs/figures/06_evaluation/06_error_analysis_by_sales_level.png`

`notebooks/07_conclusion.ipynb` 只总结这些已经得到的结果。

### 12.4 文件结构

图按 notebook 分目录：

- `outputs/figures/03_eda/`
- `outputs/figures/04_feature_engineering/`
- `outputs/figures/05_model/`
- `outputs/figures/06_evaluation/`

模型只放在 `outputs/models/`。预测文件放在 `outputs/predictions/`。

Git 忽略、不应上传：

- `data/raw/`
- `data/processed/`
- `outputs/models/`
- `outputs/predictions/`
